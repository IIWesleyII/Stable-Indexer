services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: stable_indexer
      POSTGRES_USER: stable
      POSTGRES_PASSWORD: stable
    ports:
      - "5432:5432"
    volumes:
      - stable_indexer_postgres:/var/lib/postgresql/data

volumes:
  stable_indexer_postgres:
